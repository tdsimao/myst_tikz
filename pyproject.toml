[project]
name = "myst-tikz"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "tdsimao", email = "tdsimao@users.noreply.github.com" }
]
requires-python = ">=3.11"
dependencies = [
    "jupyter-book>=2.1.6",
]

[project.scripts]
myst-tikz = "myst_tikz:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
